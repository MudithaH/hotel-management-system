# ====================================================================
# Docker Environment Variables
# ====================================================================
# This file contains environment variables for Docker Compose
# Copy this to .env in the root directory and fill in your values
# ====================================================================

# Database Configuration (for local MySQL)
# =========================================
DB_USER=root
DB_PASSWORD=root

# JWT Secret
# ==========
# IMPORTANT: Generate a secure random string
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_at_least_32_characters_long_random_string

# Notes:
# - DB_HOST is automatically set to host.docker.internal in docker-compose.yml
# - This allows containers to connect to MySQL running on your host machine
# - Ports are mapped in docker-compose.yml (frontend:3000, backend:5000)
